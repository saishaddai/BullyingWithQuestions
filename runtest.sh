#!/bin/bash
# declare run test script

#read from the setup file the path to
#check if the $BULLYNG_PATH is set
#questions files
#answers files
#bullyme.sh
#answerme.sh

#check if the alias are set rightly

#if everything was setup rightly the send a message saying so
#otherwise send a message with all the setup checks that failed
